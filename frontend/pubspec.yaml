name: frontend
description: "OmniArch Linux Software Manager Frontend"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # 需要的依赖
  web_socket_channel: ^3.0.3
  provider: ^6.1.5+1
  http: ^1.6.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true